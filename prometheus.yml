global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'reentrainement_metrics'
    static_configs:
      - targets: ['metrics-server:8002']  # Pointe vers le nouveau conteneur
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']